import React from "react";

const GithubContext=React.createContext();

export default GithubContext;



// it will provide us provider so that it will provide its values to other components that are nested inside it 

// make provider