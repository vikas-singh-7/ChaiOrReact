import { createContext } from "react";
// create context importe4d

// mnalke a contrext now basixcally context is  a object we mnake from creratecontext

const MyContext=createContext();
export default MyContext;

// context object had been made

// make a context provider
